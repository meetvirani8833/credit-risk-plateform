# Base URL of the deployed FastAPI backend. If unset, the app falls back
# to the Render URL hardcoded in src/lib/api.js, so this is optional for
# the existing deployment, set it explicitly if you redeploy the backend
# elsewhere.
VITE_API_URL=https://credit-risk-api-l0nw.onrender.com
